# Database Configuration
MONGODB_URI=mongodb+srv://Vince:emtech@emtech.tlubq5q.mongodb.net/?appName=EMTECH
PORT=5000

# JWT Configuration (for authentication)
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=24h

# Cross-Origin Resource Sharing (CORS)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8081

# Node Environment
NODE_ENV=development

# Optional initial admin credentials (ONLY use for local/dev or seeding)
# It's safer to create an admin user in the database or use a secure secret manager in production.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=emtech